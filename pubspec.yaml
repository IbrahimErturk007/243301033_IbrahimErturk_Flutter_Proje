name: okul_servis_yoklama
description: "Okul Servis Yoklama ve Sefer Sistemi - Selçuk Üniversitesi Mobil Programlama Final Projesi"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Supabase
  supabase_flutter: ^2.5.6

  # State management
  provider: ^6.1.2

  # Routing
  go_router: ^14.2.0

  # Utilities
  intl: ^0.19.0
  shared_preferences: ^2.2.3

  # Notifications
  flutter_local_notifications: ^17.2.2

  # Env
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
